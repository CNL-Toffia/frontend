"use client";

import React from "react";

export interface HeroProps {
  className?: string;
}

export default function Hero({ className }: HeroProps) {
  return (
    <section className={className}>
      {/* Hero section scaffolded stub */}
    </section>
  );
}
